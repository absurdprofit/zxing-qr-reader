{
  "name": "zxing-qr-reader",
  "version": "2.0.0",
  "description": "QR code reader based on ZXing C++ port by @nu-book compiled to wasm for use in the web.",
  "browser": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack && python3 ./post_build.py",
    "build:dev": "webpack build --mode development && python3 ./post_build.py",
    "build:prod": "webpack build --mode production && python3 ./post_build.py"
  },
  "keywords": [
    "zxing",
    "zxing-qr-reader"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/nxtexe/zxing-qr-reader.git"
  },
  "author": "nxte",
  "license": "Apache-2.0",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.3",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
